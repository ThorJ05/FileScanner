<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.*?>

<AnchorPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.example.filescanner.GUI.Controllers.ClientController"
            style="-fx-background-color: #1e1e2e;">

    <VBox spacing="20"
          AnchorPane.topAnchor="20"
          AnchorPane.leftAnchor="20"
          AnchorPane.rightAnchor="20"
          AnchorPane.bottomAnchor="20">

        <!-- TITLE ROW -->
        <HBox alignment="CENTER_LEFT">
            <Label text="Clients"
                   style="-fx-font-size: 26px; -fx-font-weight: bold; -fx-text-fill: #cdd6f4;"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Button text="Dashboard" onAction="#onBack"
                    style="-fx-background-color: #313244; -fx-text-fill: #89b4fa;
                           -fx-font-weight: bold; -fx-padding: 10 18;
                           -fx-background-radius: 8; -fx-cursor: hand;
                           -fx-border-color: #89b4fa; -fx-border-radius: 8;"/>
        </HBox>

        <!-- CREATE CLIENT + ASSIGN PROFILE -->
        <HBox spacing="10">
            <TextField fx:id="txtCompanyName" promptText="Company name"
                       style="-fx-background-color: #313244; -fx-text-fill: #cdd6f4;
                              -fx-prompt-text-fill: #585b70; -fx-border-color: #45475a;
                              -fx-border-radius: 8; -fx-background-radius: 8; -fx-padding: 10;"/>

            <Button text="Create Client" onAction="#createClient"
                    style="-fx-background-color: #89b4fa; -fx-text-fill: #1e1e2e;
                           -fx-font-weight: bold; -fx-padding: 10 18;
                           -fx-background-radius: 8; -fx-cursor: hand;"/>

            <ComboBox fx:id="comboProfiles" promptText="Select profile"
                      style="-fx-background-color: #313244; -fx-text-fill: #cdd6f4;
                             -fx-prompt-text-fill: #585b70; -fx-border-color: #45475a;
                             -fx-border-radius: 8; -fx-background-radius: 8; -fx-padding: 8;"/>

            <Button text="Assign" onAction="#assignProfile"
                    style="-fx-background-color: #a6e3a1; -fx-text-fill: #1e1e2e;
                           -fx-font-weight: bold; -fx-padding: 10 18;
                           -fx-background-radius: 8; -fx-cursor: hand;"/>
        </HBox>

        <!-- ACTIVE CLIENTS TABLE -->
        <Label text="Active Clients"
               style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #cdd6f4;"/>

        <TableView fx:id="tblClients">
            <columns>
                <TableColumn fx:id="colId" text="ID" prefWidth="80"/>
                <TableColumn fx:id="colCompany" text="Company" prefWidth="200"/>
                <TableColumn fx:id="colAssignedProfiles" text="Profiles" prefWidth="300"/>
            </columns>
        </TableView>

        <!-- DELETED CLIENTS TABLE -->
        <Label text="Deleted Clients"
               style="-fx-font-size: 18px; -fx-font-weight: bold; -fx-text-fill: #cdd6f4;"/>

        <TableView fx:id="tblDeletedClients">
            <columns>
                <TableColumn fx:id="colDeletedId" text="ID" prefWidth="80"/>
                <TableColumn fx:id="colDeletedCompany" text="Company" prefWidth="200"/>
            </columns>
        </TableView>

    </VBox>
</AnchorPane>
